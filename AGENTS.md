<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.

Project  Rules:

1. Default view = List View
2. Every module must support:
   - List View
   - Card View
3. Always use common components:
   - Table Component
   - Card Component
   - View Toggle
4. Pagination is mandatory in all list views
5. Filters must be available where applicable
6. If a new UI pattern is created → make it reusable (common component)
7. Every design make sure it has proper spacing and alignment with responsive design.
<!-- END:nextjs-agent-rules -->
